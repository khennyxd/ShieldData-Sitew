# /images/

- `logo.png` — logótipo principal, já ligado no header e no footer.
  - No footer (fundo escuro), a imagem é exibida a branco via filtro CSS (`brightness(0) invert(1)`), já que o ficheiro original é em tom escuro/marinho e ficaria invisível sobre fundo escuro.
  - Se preferires uma versão a branco feita à mão (mais fiel ao desenho original que um filtro CSS), envia `logo-branco.png` e eu troco o footer para usar o ficheiro directamente.
